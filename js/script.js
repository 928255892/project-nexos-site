// Exemplo simples de interação
document.getElementById('btnLogin').addEventListener('click', function() {
    alert('Login foi clicado!');
});

function scrollToTop() {
    window.scrollTo({
        top: 0,
        behavior: 'smooth'
    });
}
